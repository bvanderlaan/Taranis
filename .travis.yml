language: cpp
sudo: required
dist: trusty

compiler: 
  - gcc
  - clang

os:
  - linux
  - osx

script:
  - qmake Taranis.pro
  - make

addons:
  apt:
    sources:
      - ubuntu-sdk-team
    packages:
      - qt5-qmake
      - qt5-default