language: cpp

compiler: 
  - gcc
  - clang

os:
  - linux
  - osx

script:
  - qmake Taranis.pro
  - make

addons:
  apt:
    sources:
      - ubuntu-sdk-team
    packages:
      - qt5-qmake
      - qtbase5-dev